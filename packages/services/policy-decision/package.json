{
  "name": "@travel-policy/policy-decision",
  "version": "0.1.0",
  "private": true,
  "description": "Policy Decision Service",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "@travel-policy/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
