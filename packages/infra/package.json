{
  "name": "@travel-policy/infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure for the Travel Policy Platform",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist cdk.out",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@travel-policy/shared": "*",
    "aws-cdk-lib": "^2.130.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "aws-cdk": "^2.130.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
